<h2>My Bookings</h2>
      <div class="container">
    <% @bookings.each do |booking| %>
        <div class="card-booking">
          <ul>
          <li>
            <%= booking.island.name %>
            <%= booking.start_date %>
            <%= booking.end_date %>
          </li>
          </ul>
        </div>
        <% end %>
      </div>
